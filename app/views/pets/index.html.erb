
<h1>Pets</h1>

<% @pets.each do |pet| %>
  <h2><%= pet.name %></h2>
  <%=image_tag(pet.image)%>
  <ol>
    <li>Approximate Age: <%= pet.age %></li>
    <li>Sex: <%= pet.sex %></li>
    <li>Currently Located At: <%= Shelter.find(pet.shelter_id).name %></li>
  </ol>
<% end %>
